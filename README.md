# Train-Chatbot

Starting the frontend:
```
cd frontend
npm install
npx expo start
```

Backend Prerequisites:
```
fastapi
uvicorn
```

Starting the backend:
```
cd backend
uvicorn router:app --reload
```